{
  "artifact_type": "IMPLEMENTATION_BATCHES",
  "planning_session": "IMPLEMENTATION_PLANNING_20260907",
  "created_at": "2026-09-07T01:00:00Z",
  "status": "OFFLINE PLAN for a Codex-style executor. Each batch is a bounded FIX/FEATURE change set (10–30 min target). Batches do NOT authorize live execution. Business policy remains operator-owned; tests pin contracts only.",
  "batch_principle": "one contract / call path / failure mode per batch; verify-only batches reuse existing tests",
  "batches": [
    {
      "batch_id": "CODEX_BATCH_1",
      "goal": "Planner objective vs risk-gate separation + futility surfacing (quiet-futility variant pinned)",
      "cases": ["PLR-011", "PLR-007"],
      "mode": "FIX (small) + test",
      "files": ["home_policy.py", "decision_trace_schema.py"],
      "files_note": "home_policy.py S3 base branch already returns unconditional VOCAL with warning-only stamina (F-01 fixed) — verify, do not re-touch; the NEW work is (a) a futility check helper: when projected max gain over remaining weeks < remaining fan gap for the season target, decide_home returns HomeDecision('NEED_MORE_OBSERVATION'|'ESCALATE', reason='ENDGAME_FUTILE'...) instead of silently VOCAL, and (b) decision_trace gains fields rule_consuming_week + permitting_gates[] so PLANNER_JUSTIFICATION_WEAK is derivable",
      "tests": ["extend test_home_policy.py: 0906 S3 fixture (gap 31744, weeks 4, per-week VOCAL gain ~1000 → futility surfaced; gap 6525 → NOT futile so normal weeks stay VOCAL)", "extend test_decision_trace_schema.py: consuming-rule vs permitting-gates separation"],
      "risk": "LOW — additive fields + one guard branch; no existing decision path changes when target reachable",
      "expected_verification": ".venv/bin/python -m pytest test_home_policy.py test_decision_trace_schema.py -q",
      "dependency": "none",
      "duration_estimate": "25min"
    },
    {
      "batch_id": "CODEX_BATCH_2",
      "goal": "Route-deadline model verification against the 0905 multi-failure postmortem (verify-only, no new logic expected)",
      "cases": ["PLR-001", "PLR-002"],
      "mode": "VERIFY + focused test additions",
      "files": ["goal_execution.py", "home_policy.py", "test_route_deadline_policy.py"],
      "files_note": "validate_goal_deadline + route_capacity_from_state + S4 deadline branch + 10 existing tests already implement the contract; only add the postmortem-specific fixture",
      "tests": ["test_route_deadline_policy.py: S4 fixture (weeks=8, min_wins=4, observed failures=2 → capacity formula must have required 4 wins + explicit retry budget; weeks=6 boundary forces route)"],
      "risk": "VERY LOW — read-mostly verification",
      "expected_verification": ".venv/bin/python -m pytest test_route_deadline_policy.py test_goal_execution.py -q",
      "dependency": "none",
      "duration_estimate": "10–15min"
    },
    {
      "batch_id": "CODEX_BATCH_3",
      "goal": "Durability: transaction-boundary flush + evidence-in-flush + closed-run config isolation",
      "cases": ["NRG-013", "NRG-026", "NRG-012 (session fail-closed, same boundary family)"],
      "mode": "FEATURE (bounded) + F-03 hygiene",
      "files": ["new persistence helper (e.g. run_checkpoint.py) or minimal hook site in main.py transaction completion points", "config.json handling site (F-03: refuse/ignore closed-run season3 flags when active_run is null — read-only guard, no policy edit)", "enza_memory/migration/python_click_migration/distilled/runtime_recovery_requirements.json consumed as spec (do not edit)"],
      "files_note": "no durable checkpoint write exists today in main.py; implement the four boundaries from runtime_recovery_requirements (completed week / audition result / season transition / handoff) + refuse-next-transaction-on-flush-failure + tabs=0/session-lost fail-closed classification",
      "tests": ["new test_run_checkpoint.py: flush-after-week writes event+result evidence ref+digest; simulated flush failure → next week-consuming action refused; handoff boundary writes terminal state; closed-run flags ignored when active_run null (F-03)"],
      "risk": "MEDIUM — new module; keep it side-effect-free offline (pure file I/O + state machine)",
      "expected_verification": ".venv/bin/python -m pytest test_run_checkpoint.py -q + full suite green",
      "dependency": "F-03 isolation included here (it is the config half of the same durability defect)",
      "duration_estimate": "25–30min"
    },
    {
      "batch_id": "CODEX_BATCH_4",
      "goal": "Observation-failure reconciliation: post-timeout reconcile-before-retry",
      "cases": ["NRG-010"],
      "mode": "FIX (state machine) + test",
      "files": ["evidence_reconciliation.py or a small decision/executor reconciliation helper; consume learned_constraints.json C1 as spec"],
      "files_note": "POST_TIMEOUT_STATE_MUST_BE_RECONCILED_BEFORE_RETRYING_PENDING_ACTION: after observation-tool failure, pending transaction state becomes RECONCILE_PENDING; only a fresh stable state observation may resolve it to COMMITTED (no retry) or PENDING (retry allowed once); observation failure itself must not re-arm the pending action",
      "tests": ["extend test_evidence_reconciliation.py: timeout-after-click → reconcile gate; fresh state shows weeks-1 → no retry; fresh state shows no progress → single bounded retry; second timeout → STOP"],
      "risk": "LOW — pure state machine, no I/O",
      "expected_verification": ".venv/bin/python -m pytest test_evidence_reconciliation.py -q",
      "dependency": "pairs naturally after BATCH_3 (same executor loop) but is independently testable",
      "duration_estimate": "15–20min"
    },
    {
      "batch_id": "CODEX_BATCH_5",
      "goal": "Result commit guard: explicit evidence before advance; scene-skip cannot consume a business week silently",
      "cases": ["NRG-009", "RCR-006"],
      "mode": "FEATURE (bounded handler) + test",
      "files": ["result-commit module (new small handler or wing_fast_handlers.py addition; room contract enza_memory/runtime_contracts/result.json as perception spec)", "harness/vocal_harness.py only if reuse is trivial (do not refactor)"],
      "files_note": "ordering: classify explicit 合格/不合格 evidence → commit or preserve UNKNOWN → only then advance; business-week consumption requires a committed result flag; scene-skip on an uncommitted business week → STOP with SEMIFINAL_CLASS guard",
      "tests": ["new test_result_commit_guard.py: evidence-then-advance order enforced; advance-without-evidence raises/blocks; scene-skip consuming an uncommitted week → STOP; UNKNOWN result leaves flag false (already covered — re-assert integration)"],
      "risk": "LOW — offline handler + tests",
      "expected_verification": ".venv/bin/python -m pytest test_result_commit_guard.py test_season3_audition.py -q",
      "dependency": "none",
      "duration_estimate": "20–25min"
    },
    {
      "batch_id": "CODEX_BATCH_6",
      "goal": "Battle control module cleanup + Auto authority pin (unblocks dormant module before any battle integration)",
      "cases": ["NRG-004", "NRG-008", "F-07 cleanup dependency"],
      "mode": "FIX (F-07) + test tightening",
      "files": ["audition_control_epoch.py", "test_audition_control_epoch.py", "enza_memory/policies/audition_battle_speed_handler.json NOT loaded by code (policy stays; code must not import it)"],
      "files_note": "F-07: speed_action defaults target=X3 and returns CLICK_SPEED — contradicts display-only badge knowledge. Minimal fix: speed toggling becomes HOLD (return 'HOLD', 'SPEED_TOGGLE_UNGROUNDED') unless a separately grounded toggle control is injected; AUTO authority keeps existing phase/epoch contract (tests already correct)",
      "tests": ["update test_speed_epoch_rules: X2 + ungrounded toggle → HOLD not CLICK_SPEED; keep all lifecycle/epoch tests green"],
      "risk": "LOW — dormant module, no production importer (verified)",
      "expected_verification": ".venv/bin/python -m pytest test_audition_control_epoch.py test_harness_live_invariants.py -q",
      "dependency": "none; MUST precede any future battle executor integration",
      "duration_estimate": "15min"
    },
    {
      "batch_id": "CODEX_BATCH_7",
      "goal": "Choice layout_family/event_context separation in detectors",
      "cases": ["NRG-015"],
      "mode": "FEATURE (schema fields) + test",
      "files": ["shadow_executor/detectors.py", "shadow_executor/authority.py (handler gate reads both fields)", "blind_interaction_policy.py only if fingerprint naming is touched (F-11 note)"],
      "files_note": "add layout_family ∈ {THREE_CHOICE_LAYOUT_V1, TWO_CHOICE_LAYOUT_V1, UNKNOWN_CARD_LAYOUT} and event_context ∈ {MORNING, LESSON_ADVICE, AUDITION_ADVICE, PROMISE, REQUEST_DECLINE, OTHER} as separate required detection fields; handler selection requires both; missing either → fail closed",
      "tests": ["extend test_python_shadow_executor.py choice tests: 3-card AUDITION_ADVICE frame must classify layout=THREE + context=AUDITION_ADVICE (not MORNING); context missing → no handler"],
      "risk": "MEDIUM — touches detection output shape; keep fields additive to avoid breaking existing predictions",
      "expected_verification": ".venv/bin/python -m pytest test_python_shadow_executor.py test_blind_interaction_policy.py -q",
      "dependency": "F-08/F-11 relabel of event_occurrence records can be a same-batch data fix or follow-up; do not block code on it",
      "duration_estimate": "25–30min"
    },
    {
      "batch_id": "CODEX_BATCH_8",
      "goal": "Skill-board probe guards + replacement validation (shadow-level)",
      "cases": ["VNC-001/002/003", "SBR-002", "NRG-021 (merged with SBR-002 — same guard)"],
      "mode": "FEATURE (shadow detectors) + test",
      "files": ["shadow_executor/executor.py (observe_skill_board/plan_skill_action)", "recovered frames fixtures: enza_memory/migration/python_click_migration/recovered_frames/skill_board/rf_sb_g1..g4.png + replacement/rf_rp_t2_slot_list*.png"],
      "files_note": "pin: locked/dead-zone probe → NO_EFFECT/NO_PURCHASE classification; no-side-effect is never available-target evidence; replacement forget-slot must be an ORIGINAL skill (bottom-up) and confirm dialog must show expected 忘れる⇄新規 identity",
      "tests": ["new test_skill_board_probe_guards.py using recovered frame fixtures: g1 adjacency → WRONG_NODE; g2/g3 locked → NO_PURCHASE; g4 dead-zone → PANEL_CLOSE; T2 slot list → forget-slot validation rejects just-learned skill"],
      "risk": "LOW — shadow-only; fixtures are recovered frames (RECOVERED_REVIEW_PENDING review gates detector TUNING later, not logic tests)",
      "expected_verification": ".venv/bin/python -m pytest test_skill_board_probe_guards.py test_python_shadow_executor.py -q",
      "dependency": "none",
      "duration_estimate": "25–30min"
    }
  ],
  "batch_count": 8,
  "ordering_rationale": "B1/B2 (planner) first: pure logic, verify-only batch included, immediately de-risks the two P0 planning failures. B3 durability next: largest new surface, unblocks B4. B4/B5 executor-loop contracts. B6 module cleanup before any battle integration. B7/B8 shadow-detection features last: additive fields, no live-path dependency."
}
