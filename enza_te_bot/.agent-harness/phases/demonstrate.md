# 2. DEMONSTRATE / RECORD

Observe one or more real executions passively. Record ordered events, application identity, normalized targets, pre/post observations, screenshots, state/numeric evidence, branch context, duration, and annotations. Listener callbacks must not call thread-bound runtime APIs; maintain rolling pre-event observations.
