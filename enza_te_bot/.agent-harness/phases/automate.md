# 5. AUTOMATE / RUN

Replay only the reviewed model using deterministic actions, stable evidence, narrow numeric parsing, and bounded CV. Require state/action contracts, effect verification, finite retries/timeouts, and typed halts. Policy decides what; execution decides how.
